{% extends '::base.html.twig' %}
{% import '::components.html.twig' as components %}

{% block title %}
    Rules | {{ parent() }}
{% endblock %}

{% block body_def %}
    class="navbar-offset"
{% endblock %}

{% block body %}
    {{ parent() }}

    {{ components.navbar('fixed-top', 'rules') }}

    <div class="container">
        <div class="row visible-xs">
            <div class="col-md-12">
                <ul class="nav nav-tabs side-nav">
                    {% for ruleset in rulesets %}
                        <li><a href="#rule-{{ loop.index }}">{{ ruleset.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <div class="row">
            <div class="hidden-xs col-sm-3">
                <ul class="nav nav-pills nav-stacked side-nav" data-spy="affix">
                    {% for ruleset in rulesets %}
                        <li><a href="#rule-{{ loop.index }}">{{ ruleset.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            <div class="col-sm-9">
                {% for ruleset in rulesets %}
                    <section>
                        <a id="rule-{{ loop.index }}" class="offset"></a>
                        <h2>{{ ruleset.title }}</h2>
                        <div>
                            {{ ruleset.body | raw }}
                        </div>
                    </section>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}